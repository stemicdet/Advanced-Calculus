<?xml version="1.0" encoding="UTF-8"?>

<section xml:id="notes-week-09">
  <title>Week 9: The Reimann Integral (7.1-7.2)</title>

  <subsection xml:id="subsec-history">
    <title>History of the Integral</title>

    <p>
        The ancient Greek mathematicians found the areas of geometric figures using the ``method of exhaustion''. They found the areas of polygons by dividing them into triangles and adding the areas of the triangles together.
    </p>

    <p>
        The area of a circle was approximated by dividing the circle into a large number of triangles, finding the areas of the triangles and adding them together.
    </p>

    <p>
        The integral of a function as opposed to the area of a geometric figure was first considered by the early founders of calculus such as Newton and Leibniz. They viewed the integral as the inverse process of differentiation. 
    </p>

    <definition xml:id="def-newton-integral">
        <statement>
            <p>
       <term> Newton-Leibniz Integral</term>: The integral of the function <m>f</m> is the function <m>F</m> such that <m>F'(x) = f(x)</m>.
    </p>
        </statement>
    </definition>

    

    <p>
        This definition has several shortcomings. First it means that the only functions that can be differentiated are derivatives, which is a relatively small class of functions.
    </p>

    <p>
        Second since derivatives satisfy the Intermediate Value Property this means that any function with a jump discontinuity cannot be integrated.
    </p>

    <p>
        In 1823 Cauchy developed a more rigorous definition based on the idea of finding areas under a curve. It uses the concept of a partition of an interval.
    </p>

    <definition xml:id="def-partition">
        <statement>
            <p>
        A <term>partition</term> of <m>[a,b]</m> is a finite set of points from <m>[a,b]</m> that includes both <m>a</m> and <m>b</m>. The notational convention is to list the points of a partition <m>P = \{x_0, x_1, x_2, \ldots, x_n\}</m> in increasing order; thus

        <md>
            a=x_0 &lt; x_1 &lt; x_2 &lt; \ldots &lt; x_n = b
        </md>
        
    </p>
        </statement>
    </definition>

    <definition xml:id="def-cauchy-integral">
        <statement>
             <p>
        <term>Cauchy Integral</term>: Let <m>f(x)</m> be a continuous function on <m>[a,b]</m> and let <m>a = x_0 &lt; x_1 &lt; x_2 &lt; \ldots &lt; x_n = b</m>, and <m>\Delta x_k = x_k-x_{k-1}</m> for <m>k = 1,2, \ldots n</m>. The limit
        <md>
            \lim_{\textnormal{ max } \Delta x_k \to 0} \sum_{k=1}^n f(x_{k-1}) \Delta x_k
        </md>
        is called the Cauchy integral of <m>f(x)</m> over <m>[a,b]</m> and is denoted by
        <md>
            \int_a^b f(x) dx.
        </md>
        
    </p>

        </statement>
    </definition>
    

   
    <p>
        Geometrically the Cauchy integral determines the area under the curve <m>f(x)</m> by dividing <m>[a,b]</m> into rectangles of width <m>\Delta x_i</m>. The height of the rectangle is chosen to be the height of <m>f(x)</m> at the left endpoint of the rectangle.
    </p>

    <p>
        In taking the limit as the maximum width of the rectangles goes to <m>0</m> more rectangles are required to cover the area beneath the curve and as the maximum width of the rectangles tends to <m>0</m> the limit converges to the area under the curve.
    </p>

    <!-- ToDo: include a section on salamanders and their life-cycle -->


    <p>
        Because the Cauchy integral is not defined in terms of antiderivatives it allows for a larger number of functions to be integrated compared to the Newton-Leibniz integral. However, it requires that the function to be integrated be continuous which is still rather restrictive.
    </p>

    <p>
        In 1868 Bernhard Riemann developed a new way to define integration based off Cauchy's ideas that would allow for the integration of some discontinuous functions.
    </p>

    <definition xml:id="def-reimann-integral">
        <statement>
            <p>
        <term>Riemann Integral</term>: Let <m>f(x)</m> be a function on <m>[a,b]</m> and let <m>a = x_0 &lt; x_1 &lt; x_2 &lt; \ldots &lt; x_n = b</m>, and <m>\Delta x_k = x_i-x_{i-1}</m> for <m>i = 1,2, \ldots n</m>. On each subinterval <m>[x_{k-1}, x_k]</m> choose a point <m>c_k \in [x_{k-1}, x_k]</m> The limit
        <md>
            \lim_{\textnormal{ max } \Delta x_k \to 0} \sum_{k=1}^n f(c_k) \Delta x_k
        </md>
        is called the Riemann integral of <m>f(x)</m> over <m>[a,b]</m> and is denoted by

        <md>
             \int_a^b f(x) dx.
        </md>
        
        
    </p>
        </statement>
    </definition>

    

    <p>
        The sum that appears in the definition of the Riemann integral is called a Riemann sum.
    </p>

    <p>
        The difference between the Riemann integral and the Cauchy integral is that in the Cauchy integral the height of the rectangle is given by value of <m>f(x)</m> at the left endpoint, while in the Riemann integral the height of the rectangle is determined by the value of <m>f(c_k)</m> which can be any point in the subinterval <m>[x_{k-1}, x_k]</m>
    </p>

    <p>
        The value of the Riemann integral if it exists is independent of the choice of the points <m>c_k</m>. No matter what points you choose you will get the same value for the limit of the Riemann sum.
    </p>

    <p>
        The Riemann integral makes it possible to integrate a much larger set of functions than the Cauchy integral, including functions with a finite number of discontinuities; however, the Riemann integral still has a few drawbacks.
    </p>

    <p>
        The Riemann integral can't be used to integrate some functions with uncountably many discontinuities such as Dirichlet's function
        <md>
            f(x) = \begin{cases} 
      \hfill  1 \amp \text{ if $x \in \mathbf{Q}$} \\
      \hfill  0 \hfill \amp \text{ if $x \notin \mathbf{Q}$} 
  \end{cases}
        </md>
        
    </p>

    <p>
        A sequence of Riemann integrable functions can converge pointwise to a function that is not Riemann integrable.
    </p>

    <p>
        In 1904 Henri Lebesgue introduced a new definition of the integral which is now known as the Lebesgue integral. With the Lebesgue integral it is possible to integrate functions with uncountably many discontinuities, like Dirichlet's function, and the limit of a sequence of Lebesgue integrable functions is always a Lebesgue integrable function.
    </p>

    <p>
        Lebesgue integration is based on a branch of mathematics called measure theory. In order to understand the Lebesgue integral one needs to be familiar with the concepts of measure theory. For more information take MTH 650: Real Variables I.
    </p>
    
    <p>
        In 1875 Gaston Darboux developed his version of the Riemann integral which is now called the Darboux integral. The Darboux integral is equivalent to the Riemann integral; that is, a function is Darboux integrable if and only if it is Riemann integrable, and both integrals yield the same value.
    </p>

    <p>
        The advantage of the Darboux integral over the Riemann integral is that it is simpler to state and does not require the use of a limit.
    </p>

    </subsection>

    <subsection xml:id="subsec-Darboux-Integral">
        <title>The Darboux Integral</title>

         <p>
      The Darboux integral is defined using lower and upper sums.  
    </p>

    <p>
        Recall that a partition <m>P</m> of <m>[a,b]</m> is a finite set of points that includes <m>a</m> and <m>b</m>. For each subinterval <m>[x_{k-1},x_k]</m> of <m>P</m>, let 
        <md>
             m_k = \inf\{f(x): x \in [x_{k-1}, x_k] \} \textnormal{ and } M_k = \sup\{f(x): x \in [x_{k-1}, x_k] \}.
        </md>
        
    </p>

    <definition xml:id="def-lower-sum">
        <statement>
            <p>
                The <term>lower sum</term> of <m>f</m> with respect to <m>P</m> is given by
        <md>
            L(f,p) = \sum_{k=1}^n m_k(x_k-x_{k-1}).
        </md>
            </p>
        </statement>
    </definition>

<definition xml:id="def-upper-sum">
    <statement>
        <p>
            The <term>upper sum</term> of <m>f</m> with respect to <m>P</m> is given by
        <md>
            U(f,p) = \sum_{k=1}^n M_k(x_k-x_{k-1}).
        </md>
        </p>
    </statement>
</definition>

    

    <p>
        Example: Find the upper and lower sums of <m>f(x) = 4x-x^2</m> on the interval <m>[0,4]</m> with respect to the partition <m>P = \{0, 1, 3, 4\}</m>
    </p>

    <definition xml:id="def-refinement">
        <statement>
            <p>
        A partition <m>Q</m> is a <term>refinement</term> of a partition <m>P</m> if <m>Q</m> contains all the points of <m>P</m>; that is, <m>P \subseteq Q</m>.
    </p>
        </statement>
    </definition>

   

    <lemma xml:id="lem-refinement">
        <statement>
            <p>
                If <m>P \subseteq Q</m> then
                <md>
                    L(f,P) \leq L(f,Q) \leq U(f,Q) \leq U(f,P).
                </md>
                
            </p>
        </statement>
    </lemma>

    <proof>
        <p>
            The upper sum of <m>f</m> with respect to <m>Q</m> is the supremum of a set whose infimum is the lower sum of <m>f</m> with respect to <m>Q</m>, so <m>L(f,Q) \leq U(f,Q)</m>.
        </p>

         <p>
        Suppose <m>Q</m> is a refinement of <m>P</m> obtained by adding an additional point <m>z</m>. Further suppose <m>z \in [x_{K-1},x_K]</m> for some <m>K</m>.
    </p>

    <p>
        Then

        <md>
            m_K = \inf\{f(x): x \in [x_{K-1}, x_K] \}  \leq  \inf\{f(x): x \in [x_{K-1}, z]\}
        </md>
        
        and 

        <md>
             m_K = \inf\{f(x): x \in [x_{K-1}, x_K] \} \leq \inf\{f(x): x \in [z, x_K]\}
        </md>

        Let

        <md>
             m_K' = \inf\{f(x): x \in [x_{K-1}, z]\} \textnormal{ and } m_K'' = \inf\{f(x): x \in [z, x_K]\}
        </md>
        
        Then <m>m_K \leq m_K'</m> and <m>m_K \leq m_K''</m>.
        
    </p>

    <p>
        Hence

        <md>
            <mrow> L(f,P) \amp = \sum_{k=1}^n m_k (x_k - x_{k-1}) = \sum_{k=1}^{K-1} m_k (x_k - x_{k-1}) + m_K(x_K-x_{K-1}) + \sum_{k=K+1}^n m_k (x_k - x_{k-1})</mrow>
            <mrow> \amp = \sum_{k=1}^{K-1} m_k (x_k - x_{k-1}) + m_K(x_K-z)+ m_K(z-x_{K-1}) + \sum_{k=K+1}^n m_k (x_k - x_{k-1})</mrow>
            <mrow> \amp \leq  \sum_{k=1}^{K-1} m_k (x_k - x_{k-1}) + m_K'(x_K-z)+ m_K''(z-x_{K-1}) + \sum_{k=K+1}^n m_k (x_k - x_{k-1}) = L(f,Q).</mrow>
        </md>
    </p>
    </proof>

    <lemma xml:id="lem-partition-2">
        <statement>
            <p>
                If <m>P_1</m> and <m>P_2</m> are any two partitions of <m>[a,b]</m> then <m>L(f,P_1) \leq U(f, P_2)</m>
            </p>
        </statement>
    </lemma>

    <proof>
        <p>
            Let <m>P = P_1 \cup P_2</m>, which is a refinement of both <m>P_1</m> and <m>P_2</m>. Thus by the previous result, 
            <md>
                 L(f,P_1) \leq L(f,P) \leq U(f,P) \leq U(f, P_2)
            </md>
            
        </p>
    </proof>

    <p>
        An upper sum always overestimates the exact value of the integral, but by taking a refinement of the partition the upper sum decreases, meaning that it overestimates the integral by a lesser amount.
    </p>

    <p>
        Likewise a lower sum always underestimates the exact value of the integral, but by taking a refinement of the partition the lower sum decreasing so that it underestimates the integral by a lesser amount. 
    </p>

    <p>
        The idea behind the Darboux integral is that a function is integrable when the upper and lower sums converge to a common value.
    </p>

    <p>
        Let <m>\mathcal{P}</m> be the collection of all possible partitions of the interval <m>[a,b]</m>. The <term>upper integral</term> of <m>f</m> is defined to be 
        <md>
            U(f) = \inf\{U(f,P): P \in \mathcal{P}\}
        </md>
        
    </p>

    <p>
        Similarly the <term>lower integral</term> of <m>f</m> on <m>[a,b]</m> is defined to be
        <md>
             L(f) = \sup\{L(f,P): P \in \mathcal{P}\}
        </md>
        
    </p>

    <lemma xml:id="lem-upper-lower-integral">
        <statement>
            <p>
                For any bounded function <m>f</m> on <m>[a,b]</m>, it is always the case that <m>U(f) \geq L(f)</m>.
            </p>
        </statement>
    </lemma>

    <definition xml:id="def-Darboux-integral">
        <statement>
            <p>
                A bounded function <m>f</m> defined on the interval <m>[a,b]</m> is <term>Darboux Integrable</term> if <m>U(f) = L(f)</m>. In this case we define <m>\int_a^b f(x)dx</m> to be this common value; that is,
        <md>
           \int_a^b f(x) dx = U(f) = L(f) 
        </md>
            </p>
        </statement>
    </definition>

    

    <p>
        Note: The textbook uses the term Riemann integrability instead of Darboux integrability. This is not wrong because the Riemann integral and the Darboux integral are equivalent, but it is more precise to use the term Darboux integrability.
    </p>

    <p>
        To show that a function is Darboux integrable we must show that <m>U(f) = L(f)</m>. The difficulty is that computing <m>U(f)</m> and <m>L(f)</m> requires taking the supremum and infimum over all possible partitions. The next result allows us to show that <m>U(f)= L(f)</m> without needing to consider all possible partitions.
    </p>

    <theorem xml:id="thm-integrability-criteria">
        <title>Integrability Criteria</title>
        
        
        <statement>
            <p>
                A bounded function is Darboux integrable on <m>[a,b]</m> if and only if, for every <m>\epsilon>0</m>, there exists a partition <m>P_{\epsilon}</m> of <m>[a,b]</m> such that
                <md>
                    U(f, P_{\epsilon}) - L(f, P_{\epsilon}) &lt; \epsilon.
                </md>
                
            </p>
        </statement>
    </theorem>

    <proof>
        <p>
            We first prove the reverse direction. Let <m>\epsilon>0</m> be arbitrary. Then there exists <m>P_{\epsilon}</m> such that <m>U(f,P_{\epsilon}) - L(f,P_{\epsilon}) &lt; \epsilon</m>.
        </p>

        <p>
            Hence

            <md>
                U(f)-L(f) \leq U(f,P_{\epsilon}) - L(f,P_{\epsilon}) &lt; \epsilon
            </md>
            
        </p>

        <p>
            Since <m>\epsilon>0</m> is arbitrary this means that <m>U(f) = L(f)</m> and thus <m>f</m> is Darboux integrable.
        </p>

        <p>
            To prove the forward direction we will use a triangle inequality argument. However since <m>U(f) \geq L(f)</m> we will not need absolute values.
        </p>

        <p>
            Because <m>U(f)</m> is the supremum of all upper sums, given <m>\epsilon &gt; 0</m> there exists a partition <m>P_1</m> such that <m>U(f,P_1) &lt; U(f) + \frac{\epsilon}{2}</m>
        </p>

        <p>
            Likewise, there is a partition <m>P_2</m> such that <m>L(f,P_2) &gt; L(f) - \frac{\epsilon}{2}</m>.
        </p>

        <p>
            Let <m>P_{\epsilon} = P_1 \cup P_2</m> which is a refinement of both <m>P_1</m> and <m>P_2</m> and recall that since <m>f</m> is integrable <m>U(f) = L(f)</m>.
        </p>

        <p>
            Hence

            <md>
                <mrow> U(f,P_{\epsilon}) - L(f,P_{\epsilon}) \amp \leq U(f,P_1) - L(f,P_2)</mrow>
                <mrow> \amp &lt; \left(U(f)+ \frac{\epsilon}{2} \right) - \left( L(f) - \frac{\epsilon}{2} \right)</mrow>
                <mrow> \amp = \frac{\epsilon}{2} + \frac{\epsilon}{2} = \epsilon.</mrow>
            </md>
        </p>
    </proof>

    <p>
        Example: Show that <m>f(x) = 2x</m> is Darboux integrable on the interval <m>[0,3]</m>
    </p>

    <theorem xml:id="thm-darboux-integrable">
        <statement>
            <p>
                If <m>f</m> is continuous on <m>[a,b]</m> then it is Darboux integrable on <m>[a,b]</m>.
            </p>
        </statement>
    </theorem>

    <proof>
        <p>
            Since <m>f</m> is continuous on a compact set, it must be uniformly continuous on <m>[a,b]</m>.
        </p>

        <p>
            Let <m>\epsilon>0</m> be arbitrarily chosen. Then there exists <m>\delta>0</m> such that <m>|f(y)-f(z)| &lt; \frac{\epsilon}{b-a}</m> whenever <m>|y-z| &lt; \delta</m>.
        </p>

        <p>
            Now let <m>P</m> be a partition of <m>[a,b]</m> where <m>x_k - x_{k-1} &lt; \delta</m> on every subinterval <m>[x_{k-1}, x_k]</m> of <m>[a,b]</m>.
        </p>

        <p>
            On the subinterval <m>[x_{k-1},x_k]</m> the supremum <m>M_k</m> occurs at some point <m>y_k</m> and the infimum <m>m_k</m> occurs at some point <m>z_k</m>.
        </p>

        <p>
            Since <m>|y_k-z_k| &lt; \delta</m>, <m>M_k-m_k &lt; \frac{\epsilon}{b-a}</m>.
        </p>

        <p>
            Thus 

            <md>
                <mrow> U(f,P) - L(f,P) \amp = \sum_{k=1}^n M_k (x_k-x_{k-1}) - \sum_{k=1}^n m_k (x_k-x_{k-1})</mrow>
                <mrow> \amp = \sum_{k=1}^n (M_k-m_k) (x_k-x_{k-1})</mrow>
                <mrow> \amp &lt; \frac{\epsilon}{b-a} \sum_{k=1}^n (x_k-x_{k-1})</mrow>
                <mrow> \amp = \frac{\epsilon}{b-a} (x_n-x_1) = \frac{\epsilon}{b-a} (b-a) = \epsilon </mrow>
            </md>
        </p>

        <p>
            Hence <m>f</m> is Darboux integrable by the Integrability Criterion.
        </p>
    </proof>
    
    

   
        
    </subsection>

    
    </section>